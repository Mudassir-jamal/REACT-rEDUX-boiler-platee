const INITIAL_STATE = {
    label: "Login Reducer Here",
  };
  
  const reducer = (state = INITIAL_STATE, action) => {
    console.log(action)
    // switch (action.type) {
      // case "DataFromLogin"

    //   default:
        return state;
    }
//   };
  export default reducer;